<div class="space-4"></div>
<div class="space-2 contact-new">
	<h1>Say Hello!</h1>
	<div>
		<%= form_for @contact do |f| %>
			<%= f.label :name %><br>
			<%= f.text_field :name, required: true %>

			<br>

			<%= f.label :email %><br>
			<%= f.email_field :email, required: true %>

			<br>

			<%= f.label :message %><br>
			<%= f.text_area :message, as: :text %>

			<%= f.submit 'Send Message', class: 'button' %>
		<% end %>
	</div>
</div>
<div class="space-4"></div>
