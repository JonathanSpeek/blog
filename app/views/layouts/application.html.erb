<!DOCTYPE html>
<html>
  <head>
    <title>Jonathan Speek</title>
    <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
    <meta name="description" content="I'm a designer & developer from Colorado, currently doing Product Design at Cognizant Accelerator. "/>
    <meta name="keywords" content="Designer, Developer, Denver, Colorado, Jonathan Speek, HTML, CSS, Ruby, Rails, ReactJS, UX/UI, Product Thinking"/>
    <script>
      (function (i, s, o, g, r, a, m) {
        i['GoogleAnalyticsObject'] = r;
        i[r] = i[r] || function () {
          (i[r].q = i[r].q || []).push(arguments)
        },
        i[r].l = 1 * new Date();
        a = s.createElement(o),
        m = s.getElementsByTagName(o)[0];
        a.async = 1;
        a.src = g;
        m.parentNode.insertBefore(a, m)
      })(window, document, 'script', '//www.google-analytics.com/analytics.js', 'ga');

      ga('create', 'UA-66781553-1', 'auto');
      ga('send', 'pageview');
    </script>
  </head>
  <body class="<%= controller_name %>">
    <header>
      <div class="row">
        <div class="space-1 nav">
          <p class="bold"><%= link_to 'Jonathan Speek', root_path %></p>
          <ul>
            <li><%= link_to 'Posts', posts_path %></li>
            <li><%= link_to 'Projects', projects_path %></li>
            <li><%= link_to 'Contact', new_contact_path %></li>
            <% if user_signed_in? %>
              <li>
                <%= link_to 'Sign Out', destroy_user_session_path, method: :delete %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </header>

    <% if notice %>
      <p class="notice"><%= notice %></p>
    <% elsif alert %>
      <p class="alert"><%= alert %></p>
    <% end %>

    <%= yield %>

  </body>
</html>
